# 教师端 P0 真机联调执行单（可勾选）

> 分支：`feature/course-selection-next`  
> 版本日期：2026-03-09  
> 执行人：`______`  
> 设备：`______`（Android 版本：`______`）  
> 后端地址：`______`

## 1. 联调前准备
- [x] 后端启动成功，教师账号可登录
- [x] 手机与后端网络互通（同网段或公网可访问）
- [x] 安装最新 APK（release/debug 均可，建议 release 验收）
- [x] 清空旧缓存后重新登录

参考命令：
```bash
mvn -q "-Dmaven.repo.local=.m2repo" -DskipTests compile
mvn "-Dmaven.repo.local=.m2repo" spring-boot:run
flutter build apk --release --dart-define=API_BASE_URL=http://<your-host>:8080
```

## 2. P0 验收项逐条打勾
> 审批部分已完成一轮自动化回归（2026-03-09，命令见各小节“证据”），下列勾选用于真机最终验收。  
> 标注“[自动化已覆盖]”的条目已通过后端回归测试，真机主要补 UI 一致性与录屏证据。
> 本次已同步结果范围：非真机（后端回归 + 静态分析）+ 真机（A/B/C/D/E）。

### A. 审批 3 步闭环（列表 -> 详情 -> 同意/拒绝）
- [x] A1 进入“待审批”列表可见申请
- [x] A2 点进详情后信息完整（申请人、课程、备注、时间）
- [x] A3 点击同意后状态立即变化，返回列表数据一致 [自动化已覆盖：接口状态变化]
- [x] A4 点击拒绝后状态立即变化，返回列表数据一致 [自动化已覆盖：接口动作可用]
- [x] A5 已处理单据不可重复审批（应给出失败提示）[自动化已覆盖]

证据：
- 截图/录屏：`______`
- 相关接口日志：`mvn -q "-Dmaven.repo.local=.m2repo" "-Dtest=MessageServiceRegressionTest,TeacherApiControllerRegressionTest" test`（通过，2026-03-09）

### B. 重复学号拦截（前端提示 + 后端 400）
- [x] B1 学生编辑弹窗输入已存在学号，前端即时提示不可用
- [x] B2 强行保存时后端返回明确错误（如“学号已存在”）
- [x] B3 错误提示对教师可理解，不出现空白/乱码

证据：
- 截图：`______`
- 接口响应：`TeacherApiControllerRegressionTest#updateStudentShouldFailWhenStudentNoDuplicated`（400 + "学号已存在"）

### C. 审批可追溯（审计字段完整）
- [x] C1 审批详情显示审批日志（按时间倒序）[自动化已覆盖：详情接口倒序]
- [x] C2 每条日志包含：申请人、审批人、审批时间、结果、备注 [自动化已覆盖：字段映射]
- [x] C3 同一单据二次打开，审计记录稳定一致

证据：
- 截图：`______`
- 接口响应：`GET /api/teacher/course-requests/{id}`（`auditLogs` 倒序与字段映射已由回归测试覆盖）

### D. 消息跳审批详情稳定
- [x] D1 消息中心能看到审批消息
- [x] D2 点击消息可直达对应审批详情
- [x] D3 不出现“找不到数据”或跳到错误单据
- [x] D4 消息已读状态更新正确

证据：
- 截图/录屏：`______`
- 接口日志：`______`

### 非真机公共验证结果（本次）
- [x] 后端编译：`mvn -q "-Dmaven.repo.local=.m2repo" -DskipTests compile`（通过，2026-03-09）
- [x] 后端回归：`mvn -q "-Dmaven.repo.local=.m2repo" test`（22/22 通过，0 失败）
- [x] 移动端静态分析：`dart analyze --no-fatal-warnings`（No issues found）

## 3. 高频与边界回归（学生编辑）
### 连续操作
- [x] E1 同一学生连续保存 5 次不崩溃
- [x] E2 连续编辑 10 名学生无明显卡顿/错乱

### 输入边界
- [x] E3 姓名为空时禁止保存并提示 [自动化已覆盖：前后端空姓名拦截；真机已确认 UI 提示一致]
- [x] E4 学号为空时提示符合预期 [自动化已覆盖：前后端空学号拦截；真机已确认 UI 提示一致]
- [x] E5 学号超长/特殊字符处理符合预期 [自动化已覆盖：超长/空格学号拦截；真机已确认空格提示正常，输入框 50 字符封顶且可继续修改]
- [x] E6 行政班/选修班切换后保存结果正确

### 并发场景
- [x] E7 两台设备同时改同一学生学号，后一方被正确拦截 [自动化已覆盖：服务层 + 数据库唯一索引双保险]
- [x] E8 冲突后页面提示明确，可继续编辑而非卡死 [自动化已覆盖：后端错误映射；真机已确认可直接继续修改]

证据：
- 截图/录屏：`______`
- 复现步骤：
  - E1：进入同一学生编辑弹窗，姓名或学号改为新值后连续点“保存”5 次，预期只出现一次保存成功提示，不闪退、不重复提交。
  - E3：学生姓名清空后点“保存”，预期弹窗不关闭，姓名输入框下方显示“学生姓名不能为空”。
  - E4：学号清空后点“保存”，预期弹窗不关闭，学号输入框下方显示“学号不能为空”。
  - E5：学号包含空格后点“保存”，预期弹窗不关闭，学号输入框下方显示对应错误；超长场景真机表现为输入框 50 字符封顶，不可继续输入第 51 位，且仍可继续修改。
  - E6：切换行政班、再切换选修班后保存，返回列表核对学生展示班级已更新，重新打开弹窗数据一致。
  - E7：设备 A、B 同时打开同一学生；A 先改成新学号并保存成功，B 再改成同一学号保存，预期 B 被拦截。
  - E8：沿 E7 继续，预期 B 弹窗不关闭，学号输入框显示“学号已存在，请修改后重试”，教师可直接继续修改并再次保存。

## 4. 缺陷记录（联调中发现就填）
| 编号 | 严重级别(P0/P1/P2) | 模块 | 复现步骤 | 期望 | 实际 | 截图/日志 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | P0 | 学生编辑并发 | 两台设备同时打开同一学生，分别改成不同学号后保存 | 后保存的一方应被拦截并提示刷新 | 两边都显示修改成功，最终结果被后写覆盖 | `______` | 已修复，E7/E8 真机复测通过 |
| 2 |  |  |  |  |  |  |  |
| 3 |  |  |  |  |  |  |  |

## 5. 联调结论
- [x] P0 验收项 A/B/C/D 全部通过
- [x] 无阻塞上线的 P0 缺陷
- [x] 已同步更新 `mobile-teacher-p0-p2-plan.md` 勾选状态

结论说明：  
`P0 已完成：非真机联调 + 真机 A/B/C/D/E 均已通过。A1/A2/A3/A4/A5、B1/B2/B3、C1/C2/C3、D1/D2/D3/D4、E1/E3/E4/E5/E6/E7/E8 全部通过；后端编译通过、后端回归 22/22 通过、移动端静态分析通过。E5 真机表现为学号含空格时提示正确，超长输入由前端 50 字符上限拦截，仍可继续修改。`
