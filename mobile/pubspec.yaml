name: pe_teacher_app
description: 体育教师助手 - 移动端 App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.2
  go_router: ^13.0.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  image_picker: ^1.1.2
  path_provider: ^2.1.0
  open_file: ^3.3.2
  flutter_local_notifications: ^17.0.0
  workmanager: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
